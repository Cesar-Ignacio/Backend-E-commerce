<style>
    .ticket {
        background: #fff;
        border: 2px solid #ddd;
        border-radius: 8px;
        width: 300px;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .ticket-header {
        border-bottom: 2px solid #ddd;
        padding-bottom: 10px;
        margin-bottom: 10px;
    }

    .ticket-header h1 {
        font-size: 24px;
        margin: 0;
        text-align: center;
    }

    .ticket-body {
        margin-bottom: 10px;
    }

    .ticket-body p {
        margin: 5px 0;
    }

    .ticket-footer {
        border-top: 2px solid #ddd;
        padding-top: 10px;
        text-align: center;
    }

    .ticket-footer p {
        font-size: 18px;
        font-weight: bold;
        margin: 0;
    }
</style>

<div class="ticket">
    <div class="ticket-header">
        <h1>Ticket #{{ticketId}}</h1>
        <input type="hidden" name="" id="ticketId" value={{ticketId}}>
        <button id="btnPrueba">Prueba</button>
        <iframe id="pdfViewer" width="600" height="400"></iframe>
    </div>

    {{!-- <div class="ticket-body">
        <p><strong>Cliente:</strong> {{purchaser}}</p>
        <p><strong>Fecha:</strong> {{purchase_datetime}}</p>
        <p><strong>Descripción:</strong> Compra de productos varios</p>
    </div>
    <div class="ticket-footer">
        <p><strong>Total:</strong> ${{amount}}</p>
    </div> --}}
</div>

<script src="/static/js/ticket/generateTicket.js"></script>